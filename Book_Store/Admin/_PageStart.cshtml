@{
   if (!Roles.IsUserInRole(WebSecurity.CurrentUserName, "Administration"))
    {
        string currentUrl = Request.Url.ToString();
        Response.Redirect("~/My_Account?sender=" + currentUrl);
    }
}
